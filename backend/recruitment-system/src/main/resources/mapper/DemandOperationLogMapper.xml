<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hr.mapper.DemandOperationLogMapper">

    <insert id="insert" parameterType="com.hr.entity.DemandOperationLog" useGeneratedKeys="true" keyProperty="logId">
        INSERT INTO demand_operation_log (
            demand_id,
            operation_type,
            operation_detail,
            operator_user_id,
            operator_user_name,
            operation_time
        ) VALUES (
            #{demandId},
            #{operationType},
            #{operationDetail},
            #{operatorUserId},
            #{operatorUserName},
            #{operationTime}
        )
    </insert>

</mapper>
