# 服务器配置
server.port=8080

# 数据库连接配置（先连接到MySQL服务器创建数据库）
spring.datasource.url=jdbc:mysql://localhost:3306/hr_system?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=p@ssw0rd
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# 数据库初始化配置（禁用自动初始化，手动创建数据库）
# spring.datasource.initialization-mode=always
# spring.sql.init.mode=always
# spring.sql.init.continue-on-error=true

# MyBatis配置
mybatis.type-aliases-package=com.hr.entity
mybatis.mapper-locations=classpath:mapper/*.xml

# 日志配置
logging.level.com.hr=debug
